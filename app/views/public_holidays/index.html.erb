<h1>Listing public_holidays</h1>

<table>
  <tr>
    <th>Day</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @public_holidays.each do |public_holiday| %>
  <tr>
    <td><%= public_holiday.day %></td>
    <td><%= public_holiday.event %></td>
    <td><%= link_to 'Show', public_holiday %></td>
    <td><%= link_to 'Edit', edit_public_holiday_path(public_holiday) %></td>
    <td><%= link_to 'Destroy', public_holiday, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Public holiday', new_public_holiday_path %>
